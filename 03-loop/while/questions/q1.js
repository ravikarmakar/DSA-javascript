// Q1. write a programe using while condition matching there conditon then break the loop.

let input = prompt("Write 'exit' to break loop");

while (input !== "exit") {
  input = prompt("Write 'exit' to break loop");
}
